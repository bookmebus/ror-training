<%= form_with(model: v_quote) do |form| %>
  <% if v_quote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(v_quote.errors.count, "error") %> prohibited this v_quote from being saved:</h2>

      <ul>
        <% v_quote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quote, style: "display: block" %>
    <%= form.text_area :quote %>
  </div>

  <div>
    <%= form.label :authur, style: "display: block" %>
    <%= form.text_area :authur %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
